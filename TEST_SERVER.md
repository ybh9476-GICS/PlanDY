# 테스트 서버 최신 파일 표시 원칙

## 요구 사항

- 로컬 테스트 서버는 HTML, CSS, JavaScript의 최신 파일을 항상 제공해야 한다.
- Codex 테스트 브라우저가 이전 파일을 캐시로 재사용하지 않도록 개발용 서버에서 캐시를 사용하지 않거나, 최신 파일을 확실히 다시 받도록 구성한다.
- 기능을 수정한 뒤에는 `start-local-editor.cmd`로 서버를 실행하고 `http://127.0.0.1:4173/`에서 최신 기능이 표시되는지 확인한 후 커밋 또는 푸시한다.

## 권장 작업 흐름

1. 기능을 수정한다.
2. 최신 파일과 로컬 첨부파일 저장 기능을 제공하는 서버에서 4173 화면을 연다.
3. 실제 화면에서 변경 기능을 확인한다.
4. 확인이 끝난 뒤에만 Git 스테이지, 커밋, 푸시를 진행한다.

## 참고

이 문서는 테스트 서버의 목표와 작업 기준을 기록한다. 캐시 방지 서버 설정 자체는 별도 작업으로 구현한다.
