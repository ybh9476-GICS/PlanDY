# WMS 기획 프로젝트 작업 규칙

## 1. 적용 범위와 기본 원칙

- 이 파일의 지침은 `D:\work_Antigravity\DT_01` WMS 기획 프로젝트에만 적용한다.
- 사용자가 지정한 메뉴와 기능만 수정한다.
- 테스트 메뉴를 수정할 때 개요·경로 찾기 메뉴의 저장 데이터와 화면 동작을 변경하지 않는다.
- 개요 또는 경로 찾기 메뉴를 수정할 때 테스트 메뉴의 카드 기능과 저장 데이터를 변경하지 않는다.
- 공통 카드 편집기, 권한 제어, 전역 이벤트 또는 공통 스타일을 수정할 때는 테스트·개요·경로 찾기 메뉴의 영향을 모두 확인한다.
- 작업 전 관련 코드와 이벤트를 분석하고, 변경 전 회귀 테스트를 실행한다.
- 기존 테스트가 실패하면 기능을 적용하지 않고 실패 원인을 보고한다.
- 기능 적용 후 같은 회귀 테스트를 다시 실행하고, 브라우저에서 실제 사용자 동작을 확인한다.
- 기존 기능이 유지되지 않으면 완료로 보고하지 않는다.

### 콘텐츠 생성과 카드 등록 요청 분리

- 사용자가 자료 조사, 용어 정리, 표 작성, 이미지 생성만 요청한 경우에는 결과를 대화와 요청 범위의 파일에만 제공하고, 화면의 카드·메뉴·브라우저 저장 데이터에는 등록하지 않는다.
- 카드·메뉴·화면 등록은 사용자가 `카드에 등록`, `메뉴에 넣어줘`, `특정 카드에 추가`처럼 등록 대상 또는 등록 행위를 명시한 경우에만 수행한다.
- 이전 대화에서 카드 등록을 수행했더라도, 새 요청에 등록 지시가 없으면 그 맥락을 자동으로 이어 적용하지 않는다.
- 등록 요청이 명시된 경우에도 대상 메뉴·카드가 불명확하면 저장 데이터를 변경하기 전에 대상 확인을 요청한다.

### 내보낸 사이트 콘텐츠의 Git 반영

- 사용자가 메뉴 관리의 `콘텐츠 JSON`에서 내보낸 `site-content.json` 파일을 채팅에 첨부하고 `스테이지`, `커밋`, `푸시`를 요청하면, 첨부 파일을 프로젝트의 `data/site-content.json`으로 반영한 뒤 Git에 포함한다.
- 이 요청은 브라우저의 `가져오기` 기능을 실행하는 요청이 아니다. 명시적으로 브라우저 가져오기를 요청하지 않는 한, 현재 브라우저의 메뉴·카드·이미지 저장 데이터를 덮어쓰지 않는다.
- 첨부 파일이 다운로드 폴더 등 프로젝트 밖에 있더라도, `git status`가 깨끗하다는 이유만으로 푸시할 내용이 없다고 판단하지 않는다. 첨부 파일과 반영 대상 `data/site-content.json`을 비교해 반영 여부를 판단한다.
- 반영 전에는 JSON 형식, 메뉴·카드 변경 요약, 이미지 참조를 확인한다. 반영 후에는 JSON 유효성, 관련 테스트, `git diff`를 확인한 뒤 스테이지·커밋·푸시한다.
- 사용자가 `가져오기` 또는 `적용`만 요청해 대상이 모호한 경우에는 브라우저 저장 데이터 적용인지 `data/site-content.json` Git 반영인지 먼저 확인한다.

## 2. 필수 작업 절차

1. 요청 범위와 수정 대상 파일을 확인한다.
2. 현재 동작, 저장 키, 관련 이벤트를 분석한다.
3. 변경 전 `npm.cmd test`를 실행한다.
4. 테스트가 모두 통과한 경우에만 최소 범위로 수정한다.
5. 호환 진입 페이지에 영향을 주는 `index.html` 변경이면 `npm.cmd run sync:pages`를 실행한다.
6. 변경 후 `npm.cmd test`를 다시 실행한다.
7. 브라우저에서 Viewer와 Editor의 실제 동작을 확인한다.
8. 수정 파일, 수정 범위, 변경 전·후 테스트 결과, 브라우저 확인 결과를 보고한다.

## 2.1. 재현 기반 문제 해결과 완료 판정

- 사용자가 보고한 조건을 변경 전 화면에서 먼저 재현한다.
- 변경 전 실패를 재현하지 못하면 추측으로 코드를 수정하거나 문제 해결을 완료로 보고하지 않는다.
- 사용자가 제시한 입력 방식, 브라우저, 권한, 메뉴, 데이터 상태를 동일하게 사용한다.
- 자동 입력과 실제 사용자 입력의 동작이 다를 수 있는 기능은 동일한 테스트로 간주하지 않는다.
- 한글 IME, 드래그, 클립보드, 파일 업로드, 맞춤법 검사 등 운영체제 또는 브라우저 입력 기능은 단순 DOM 값 삽입으로 검증하지 않는다.
- 테스트 환경에서 정확한 재현 조건을 만들 수 없으면 `재현하지 못함`으로 보고하고, 확인하지 않은 기능을 정상 또는 수정 완료로 판정하지 않는다.
- 수정은 변경 전 실패가 확인된 뒤에만 적용한다.
- 변경 후에는 변경 전과 정확히 같은 절차로 다시 테스트한다.
- 완료 조건은 `변경 전 실패 → 변경 후 성공`이 모두 증거로 확인되는 것이다.
- 코드가 존재하거나 이벤트 리스너가 추가된 사실만으로 기능이 동작한다고 판정하지 않는다.
- 자동화 테스트가 통과하더라도 사용자가 보고한 실제 입력 조건이 검증되지 않았다면 완료로 보고하지 않는다.
- 테스트 결과에는 입력값, 조작 순서, 실제 결과, 기대 결과, 재현 여부를 기록한다.
- 테스트하지 못한 항목과 테스트 도구의 한계를 명확히 보고한다.

## 2.2. 자동 등록·카드 편집기 저장 충돌 방지

- 카드·테이블·텍스트 본문·이미지 등 어떤 형태의 자동 등록도 카드 편집기의 초기화 또는 기존 데이터 저장보다 먼저 완료되어야 한다.
- 자동 등록이 비동기로 저장되는 동안에는 대상 메뉴의 카드 편집기가 기존 화면 데이터를 다시 저장하거나 렌더링 결과를 저장하지 않아야 한다.
- 자동 등록이 완료되면 저장된 카드 데이터를 다시 읽어 화면을 구성해야 하며, 이전에 메모리에 있던 카드 행·본문·표 데이터를 재사용해서는 안 된다.
- 자동 등록 처리 중 오류가 발생하면 적용 완료 표식(패치 완료 표시)을 저장하지 않는다. 오류 원인과 대상 카드·메뉴를 확인할 수 있어야 한다.
- 자동 등록 후 검증은 제목·설명·텍스트 본문·표 셀 값·이미지 블록·썸네일 이미지·카드 순서를 모두 실제 화면에서 확인한다.
- 이미지 로드 여부, 요소 개수, 대체 텍스트만으로 등록 성공을 판정하지 않는다. 변경 전 값과 변경 후 기대값을 실제 카드 화면에서 비교한다.
- 텍스트 기반 본문은 자동 등록 뒤 줄바꿈, 목록, 서식, 링크, 표가 기존 데이터로 되돌아가지 않았는지 확인한다.
- 자동 등록으로 수정한 대상 카드 외의 카드·메뉴 데이터가 변경되지 않았는지 저장 데이터와 화면에서 함께 확인한다.

### 한글 IME와 Enter 검증

- 한글 조합 밑줄이 실제로 표시된 상태에서 Enter를 한 번 눌러 검증한다.
- 첫 Enter로 한글 조합 확정과 줄바꿈이 모두 발생해야 한다.
- 자동으로 완성된 한글 문자열을 삽입하는 테스트는 실제 IME 입력 검증으로 인정하지 않는다.
- 테스트 환경에서 실제 IME 조합 상태를 만들 수 없으면 수정 완료로 보고하지 않는다.

## 3. 권한 모드 보호

### 공통 원칙

- 사이트는 로그인 전 로그인 화면만 표시하며, 아이디·비밀번호가 각각 영문·숫자 정확히 8자로 입력된 경우에만 로컬 테스트 로그인을 허용한다. 허용되지 않는 문자는 입력·붙여넣기 시 제거하고, 한글 조합이 시작되면 `한글 입력 중 — 영문으로 전환하세요` 안내를 표시한다. 기본 길이, 규칙 위반, 입력 완료 상태를 각 필드에 표시하고, 비밀번호는 저장하지 않으며 로그인 아이디와 권한만 현재 브라우저 세션에서 유지한다.
- 사이트의 기본 권한은 Viewer다.
- 로그인 후 권한 상태는 사이드바 최하단의 `아이디 · Viewer` 또는 `아이디 · Editor` 버튼으로 표시한다.
- 권한 상태 버튼을 누르면 로그인 아이디, Viewer와 Editor 선택, `확인`, `취소`를 제공하는 팝업을 표시한다. 팝업 하단의 `로그아웃`은 확인창을 거친 뒤 로그인 아이디와 권한만 지우고 로그인 화면으로 돌아간다.
- 선택한 권한은 현재 브라우저 세션에서만 유지한다. 새 세션은 로그인 화면에서 시작하며, 로그인하면 Viewer로 시작한다.
- Viewer에서는 편집 UI를 숨기는 것뿐 아니라, 카드 추가·수정·순서 변경·메뉴 관리 실행 경로도 차단한다.
- Editor에서만 메뉴 관리, 카드 추가, Edit, 카드 `↑`·`↓` 버튼을 표시하고 동작시킨다.
- 권한 선택 팝업은 Esc와 배경 클릭으로 취소할 수 있고, 닫힌 뒤에는 권한 상태 버튼으로 포커스를 돌린다.

### 메뉴 관리

- 사이드바의 `메뉴 관리`는 Editor에게만 표시한다.
- Viewer는 메뉴 관리 팝업을 열거나 메뉴를 추가·수정·삭제·정렬할 수 없어야 한다.

## 4. 공통 카드 편집기와 저장 데이터

- 카드 편집기는 `initializeSharedCardEditor`를 공통으로 사용한다.
- 메뉴별 카드 저장 키는 반드시 분리한다.

- 테스트: `wms-test-menu-cards-v1`
- 개요: `wms-overview-cards-v1`
- 저작도구: `wms-authoring-cards-v1`
- 경로 찾기: `wms-route-cards-v1`

- 카드 순서 변경은 카드 메타데이터를 함께 이동해야 한다. 이미지, 표, 구조화 콘텐츠, `editLocked` 상태가 분리되면 안 된다.
- 신규 메뉴를 포함한 모든 메뉴는 동일한 공통 카드 편집기 기능을 사용한다.
- 모든 메뉴는 Editor 권한에서 동일하게 카드 추가, 1·2·3열 카드 생성, 수정, 삭제, 순서 변경, 이미지·본문 블록 추가, 표 편집, 카드복사·붙여넣기를 지원한다.
- 본문 블록 타입은 `텍스트`, `테이블`만 제공한다. 기존 `plainText` 데이터는 편집기를 열 때 `텍스트` 블록으로 안전하게 전환해 콘텐츠를 보존한다.
- 메뉴별 카드 데이터는 분리해 저장하되, 편집 기능과 권한 동작은 메뉴마다 달라지면 안 된다.
- 저장된 기존 카드 데이터는 호환성을 유지한다. 기존 카드 형식이 공통 편집기 형식과 다를 경우, 기존 콘텐츠가 손실되지 않도록 해당 메뉴의 저장 데이터만 안전하게 마이그레이션한다.
- 모든 사용자 추가 메뉴에도 `structuredContent: true`를 기본 적용한다.
- 표 편집기의 열 최소 너비는 36px이며, 마지막 열도 인접 열과 너비를 교환해 같은 최소 너비까지 조절할 수 있어야 한다. 셀은 여러 줄 입력을 지원하고, 긴 텍스트·줄바꿈으로 필요한 높이가 늘어나면 해당 행도 함께 확장되어야 한다.
- Mermaid·직접 SVG 등 카드 수정 팝업에서 구성하거나 안전하게 저장할 수 없는 콘텐츠는 `Edit`을 비활성화한다. 이 경우에도 카드 조회와 순서 변경은 유지한다.

## 5. 테스트 메뉴 카드 기능 보호

### 카드 생성과 정렬

- Editor일 때만 테스트 메뉴 본문과 카드 아래에 카드 추가 버튼을 표시한다.
- 1열 카드 타입은 본문 전체 폭의 카드 1개를 생성한다.
- 2열 카드 타입은 같은 행에 카드 2개를 생성한다.
- 카드 타입 선택 후 등록 버튼이 활성화된다.
- 생성된 빈 카드에는 `빈 카드`이 표시된다.
- 2열 카드에서 한 카드를 삭제하면 남은 카드는 1열 카드로 변경된다.
- Editor일 때 각 카드의 `↑`, `↓` 버튼으로 읽기 순서 기준 한 단계씩 이동할 수 있다.
- Viewer에서는 카드 추가와 `↑`, `↓` 버튼을 표시하지 않는다.

### 카드 수정 팝업

- 편집 가능한 카드는 Editor일 때 우측 상단에 `Edit` 버튼을 표시한다.
- 팝업 제목은 `카드 수정`으로 표시한다.
- 닫기, 등록, 삭제 버튼을 유지한다.
- 헤더, 헤더 설명, 이미지 슬롯, 본문 입력 영역의 기본 순서를 유지한다.
- 브라우저 높이에 맞춰 팝업을 표시하고 작은 화면에서는 내부 스크롤을 제공한다.
- 팝업 외부 드래그나 클릭으로 전역 업데이트 등록 버튼이 발생하지 않게 한다.
- 팝업 내부와 팝업에서 연 보조 창만 상호작용을 허용한다.
- 보조 창에는 글머리 기호 선택 메뉴, 인라인 서식 툴바, 삭제 확인창, 원본 이미지 보기 창이 포함된다.
- 팝업 종료 시 팝업 전용 이벤트 리스너를 반드시 제거한다.

### 삭제 조건

- 제목, 설명, 이미지, 본문이 모두 비어 있고 새 입력도 없으면 즉시 삭제한다.
- 저장된 값이 하나라도 있거나 편집 흔적이 있으면 다음 확인 문구를 표시한다.

  `작성된 내용이 있습니다. 삭제하겠습니까?`

- 확인창에는 `삭제`와 `취소` 버튼을 표시한다.

## 6. 개요 메뉴 카드 보호

- 개요 메뉴는 카드 목록과 개요 전용 카드 추가 버튼으로 구성한다.
- 기본 콘텐츠는 WMS 기획 개요, 3D 안내 이미지, 핵심 용어 표 카드로 제공한다.
- Editor는 카드 추가·수정·삭제·순서 변경을 할 수 있다.
- Viewer는 저장된 카드만 볼 수 있다.
- 표 카드는 수정 팝업에서 제목, 행·열, 헤더, 셀 값, 행·열 크기를 편집할 수 있어야 한다.
- 기존 개요의 정적 콘텐츠는 삭제하지 않고 숨김 보존한다.

## 7. 저작도구 메뉴 카드 보호

- 저작도구 메뉴는 카드 목록과 저작도구 전용 카드 추가 버튼으로 구성한다.
- 기본 콘텐츠는 3D Canvas Viewport, 랙 타입 및 규격 설정, 프리팹 및 변환 제어, 워크플로·편집 명세 표 카드로 제공한다.
- Editor는 저작도구 카드의 추가·수정·삭제·순서 변경을 할 수 있다.
- Viewer는 저장된 저작도구 카드만 볼 수 있다.
- 저작도구의 표 카드는 수정 팝업에서 제목, 행·열, 헤더, 셀 값, 행·열 크기를 편집할 수 있어야 한다.
- 기존 정적 저작도구 화면은 삭제하지 않고 숨김 보존한다.

## 8. 경로 찾기 메뉴 카드 보호

- 경로 찾기 메뉴는 카드 목록과 경로 찾기 전용 카드 추가 버튼으로 구성한다.
- 기본 콘텐츠는 다음 카드로 제공한다.

  1. 5단계 경로 탐색 운용 시퀀스 흐름도
  2. 실시간 3D 경로 탐색 이미지
  3. 경로 기점 및 Location 표

- 1번 AI 생성 흐름도 카드는 `editLocked: true`를 저장한다.
- 잠금 흐름도 카드는 Mermaid 다이어그램 블록으로 렌더링하며, Mermaid 렌더링 실패 시 원본 다이어그램 소스를 읽기 전용으로 표시한다.
- Editor에서도 잠금 카드의 Edit 버튼은 표시하지만 비활성화한다.
- 잠금 Edit 버튼에는 다음 안내를 유지한다.

  `AI 생성 흐름도 카드로 수정할 수 없습니다.`

- 잠금 카드도 Editor의 `↑`, `↓` 순서 변경은 허용한다.
- 잠금 카드는 Edit 팝업을 열 수 없으므로 수정·삭제할 수 없다.
- 일반 경로 찾기 카드는 Editor가 추가·수정·삭제·정렬할 수 있다.
- 경유지 표 카드는 구조화된 표 콘텐츠로 저장하고 수정 팝업에서 편집할 수 있어야 한다.
- 기존 경로 찾기의 정적 콘텐츠는 삭제하지 않고 숨김 보존한다.

## 9. 본문 편집과 서식 기능 보호

### 번호와 글머리 기호

- `1. 첫 문장` 입력 후 Enter를 누르면 다음 줄은 `2. `로 시작한다.
- Tab으로 들여쓰기하면 하위 번호는 `1. `부터 시작한다.
- Shift+Tab으로 내어쓰기하면 같은 레벨의 번호 또는 기호 흐름을 이어받고, 번호가 중복되면 안 된다. 최상위 번호·글머리 기호 줄에서 Shift+Tab을 누르면 접두어를 제거해 일반 텍스트로 전환한다. 여러 줄을 선택한 경우에도 같은 규칙을 각 줄에 적용한다.
- 다음 글머리 기호를 유지한다: `•`, `◦`, `-`.
- 글머리 기호 문장에서 Enter를 누르면 같은 기호가 다음 줄에 이어진다.

### 목록 선택과 커서

- 본문에서 `/` 입력 시 목록 선택 메뉴를 표시한다.
- 메뉴 항목 순서는 `1`, `•`, `◦`, `-`이며, 좌우 방향키와 Enter로 선택한다.
- `/` 목록 선택 메뉴와 서식 툴바는 Esc로 닫을 수 있다.
- Tab 또는 Shift+Tab 뒤에도 선택 상태가 남지 않고, 이후 입력이 기존 텍스트를 삭제하면 안 된다.
- Enter, 방향키, 마우스 선택, 여러 줄 입력·삭제 뒤에도 목록 구조와 입력 가능한 커서를 유지한다.

### 서식과 URL

- 드래그 선택 시 선택 영역 근처에 인라인 서식 툴바를 표시한다.
- 서식 툴바에는 `Bold`, `Regular`와 다음 색상을 유지한다.

  - 검정 `#000000`
  - 회색 `#999999`
  - 빨강 `#ff0000`
  - 주황 `#ff9900`
  - 파랑 `#0000ff`
  - 분홍 `#ff00ff`

- 선택한 텍스트에만 서식을 적용한다.
- 등록 뒤에도 줄바꿈, 목록, 서식, URL 링크가 카드에 동일하게 표시되어야 한다.

## 10. 이미지와 URL 보호

- 이미지는 드래그앤드롭, 파일 업로드, Ctrl+V 붙여넣기를 지원한다.
- 등록 이미지마다 독립된 이미지 프레임을 사용하고, 마우스를 올리거나 버튼에 키보드 포커스가 있을 때 해당 이미지 내부 우측 상단에 `원본보기` 버튼을 표시한다.
- `원본보기` 버튼은 이미지 프레임의 자식으로 유지하여, 이미지에서 버튼으로 포인터를 옮겨도 표시 상태와 클릭 가능 상태가 유지되어야 한다.
- 카드 본문과 카드 수정 팝업에서 원본 이미지 보기 동작을 유지한다.
- URL 렌더링은 Bold, Regular, 색상, 줄바꿈 구조를 손상시키지 않는다.

## 11. 사이드바 브랜드 보호

- 사이드바 레이아웃과 접기 동작은 변경하지 않는다.
- 현재 제목은 `기획 베포 테스트`, 설명은 `기획 배포 테스트 R&D`로 표시한다.
- 제목은 기존 로고 높이에 맞춘 굵은 텍스트를 유지한다.
- 기존 YUPOONG SVG 로고와 `유풍 창고 관리 시스템(WMS)` 설명은 복구할 수 있도록 마크업에 보존한다.
- 로그인 화면과 사이드바의 제목·설명은 `body`의 `data-brand-title`, `data-brand-description`을 단일 기준으로 사용한다. 한쪽 브랜드 필드가 실행 중 변경되면 다른 필드도 즉시 같은 값으로 동기화한다.

## 11.1 도움말 메뉴 보호

- 사이드바의 `도움말`은 `업데이트` 바로 아래, 권한 설정 바로 위에 표시하며 Viewer와 Editor 모두 사용할 수 있다.
- 도움말은 화면 구성, 카드 구성·생성, 카드 수정, 텍스트·테이블 작성 방법을 계층 구조로 제공한다.
- Editor 전용 기능은 도움말 본문에서 `Editor` 태그로 구분한다.
- 도움말 팝업은 닫기 버튼, `확인` 버튼, Esc, 배경 클릭으로 닫히며 로그인 화면에서는 표시되지 않는다.
- 도움말의 레벨 1과 레벨 2 항목은 `▶`·`▼` 토글 버튼으로 하위 내용을 열고 닫는다. 레벨 1은 한 항목만 열리는 아코디언이고, 레벨 2는 같은 레벨 1 안에서 여러 항목을 동시에 열 수 있다. 최초에는 `1. 화면 구성`과 `1.1 사이드바`만 펼친다.

## 12. 구현 제한

- 기존 본문 입력 영역을 투명 레이어로 덮거나 복제 영역에 동기화하지 않는다.
- 기존 입력 이벤트, 커서, 선택 영역을 임의로 대체하지 않는다.
- 저장된 본문 HTML과 일반 텍스트를 모두 안전하게 처리한다.
- 새 이벤트 리스너는 관련 팝업 또는 세션 종료 시 반드시 제거한다.
- 기능 추가를 위해 기존 목록 처리 로직을 재작성하지 않는다.
- 공통 카드 편집기 변경은 옵션과 메타데이터로 범위를 제한하고, 메뉴 ID에 임시로 묶지 않는다.
- Viewer 상태에서는 CSS 숨김에만 의존하지 말고 JavaScript 실행 경로도 권한을 확인한다.
- 전역 `업데이트 등록` 버튼은 로그인 완료 상태의 Editor에게만 표시·실행된다. 로그인 화면과 Viewer에서는 텍스트를 선택해도 표시되지 않으며, Viewer 전환·로그아웃 시 표시된 버튼과 열린 등록 팝업은 즉시 닫힌다. 버튼 클릭과 실제 등록 실행 경로도 동일한 권한을 다시 확인한다.

## 12.1. 흐름도·도식화·다이어그램 기본 출력 규칙

- 사용자가 `흐름도를 만들어줘`, `도식화해줘`, `다이어그램을 만들어줘`라고 요청하면 기본적으로 비트맵 이미지가 아닌 벡터 기반 다이어그램으로 작성한다.
- 벡터 기반 다이어그램은 Mermaid 렌더링 SVG 또는 직접 작성한 SVG 등, 확대해도 품질이 유지되는 형식을 사용한다.
- 사용자가 이미지, 사진, 일러스트, 비트맵 등 이미지 형식을 명시적으로 요청한 경우에만 래스터 이미지를 생성하거나 사용한다.
- 카드 수정 팝업에서 구성·저장할 수 없는 다이어그램 콘텐츠가 포함된 카드는 `Edit` 버튼을 비활성화하고, 카드 순서 변경 기능은 유지한다.

## 13. 회귀 테스트

변경 전과 변경 후에 `npm.cmd test`를 실행한다. 카드·권한·공통 편집기를 수정하면 아래 브라우저 검증도 수행한다.

1. Viewer 최초 진입 시 메뉴 관리, 카드 추가, Edit, `↑`, `↓`가 표시되지 않는지 확인
2. Editor 전환 후 메뉴 관리, 카드 추가, Edit, `↑`, `↓`가 표시되고 동작하는지 확인
3. Viewer 복귀 후 편집 제어가 즉시 숨겨지고 실행 경로가 차단되는지 확인
4. 로그인 전 해시 메뉴 직접 접근 시 로그인 화면만 표시되고, 아이디·비밀번호가 각각 영문·숫자 8자가 되기 전에는 로그인 버튼이 비활성인지 확인
5. 아이디·비밀번호 필드에서 한글 조합 시작 → `한글 입력 중 — 영문으로 전환하세요` 표시, 조합 종료 후 규칙 위반 안내와 영문·숫자 입력 뒤 길이·완료 안내가 갱신되는지 확인
6. 영문·숫자 8자의 아이디·비밀번호 입력 후 로그인 → 아이디가 사이드바 하단에 `아이디 · Viewer`로 표시되고 현재 해시 메뉴가 열리는지 확인
7. 권한 팝업의 로그아웃 → 확인창의 확인 후 로그인 화면 복귀, Viewer 초기화, 카드·메뉴 데이터 유지 확인
8. 테스트 메뉴에서 `1. 첫 번째` 후 Enter → `2. ` 확인
9. 테스트 메뉴에서 `• 첫 번째` 후 Enter → `• ` 확인
10. `/` 입력 → 4개 항목 표시, 방향키 이동·Enter 선택·Esc 닫기 확인
11. 번호·글머리 기호 줄에서 Tab과 Shift+Tab 뒤 목록 흐름과 기존 텍스트가 유지되는지 확인
12. 선택 텍스트에 Bold, Regular, 6가지 색상 적용과 Esc 닫기 확인
13. 등록 후 줄바꿈, 목록, 서식, URL 링크가 카드에 동일하게 표시되는지 확인
14. 카드 수정 팝업 외부 드래그가 전역 업데이트 등록 버튼을 발생시키지 않는지 확인
15. 카드 수정 팝업 닫기 버튼과 이벤트 리스너 정리가 정상인지 확인
16. 개요의 표 카드가 수정 팝업에서 표 편집기를 표시하고 저장 뒤에도 표를 유지하는지 확인
17. 경로 찾기의 Mermaid 흐름도 카드가 렌더링되거나 읽기 전용 원본으로 표시되는지 확인
18. 경로 찾기 잠금 카드의 Edit가 비활성이고 `↑`, `↓`는 활성인지 확인
19. 잠금 카드와 일반 카드의 순서를 바꾼 뒤 `editLocked` 상태가 유지되는지 확인
20. `[텍스트]`에서 여러 일반 문장을 작성한 뒤 기존 줄 끝에 커서를 옮겨 Enter 한 번 → 커서 뒤 콘텐츠가 정확히 한 줄만 이동하고 임시 `<br>`이 삽입되지 않는지 확인
21. `[텍스트]`의 본문 마지막에서 Enter 한 번 → 다음 빈 줄의 커서가 표시되고 저장 HTML에는 임시 `<br>`이 포함되지 않는지 확인
22. `[텍스트]`의 번호 목록 중간에서 Enter로 같은 레벨 번호 줄 추가 → 새 줄 아래의 연속된 같은 레벨 번호가 순서대로 증가하고, 하위 번호·글머리 기호·일반 문장은 변경되지 않는지 확인
23. `[텍스트]`의 최상위 번호·글머리 기호 줄에서 Shift+Tab → 접두어만 제거되어 일반 텍스트가 되고, 번호 목록 중간에서 전환한 경우 아래 같은 레벨 번호가 재정렬되는지 확인
24. 기존 `plainText` 블록이 있는 카드를 열어 등록 → 내용과 인라인 서식을 유지한 `텍스트` 블록으로 저장되는지 확인
25. 이미지가 있는 카드에서 이미지 위로 포인터 이동 → `원본보기`가 해당 이미지 내부 우측 상단에 표시되고, 버튼으로 포인터를 이동한 뒤 클릭해도 원본 이미지 창이 열리는지 확인
26. 표 편집기에서 각 열과 마지막 열을 36px까지 축소할 수 있고, 인접 열은 36px 미만으로 줄지 않는지 확인
27. 표 셀에 셀 폭보다 긴 한글·영문 텍스트 입력 → 자동 줄바꿈과 행 높이 확장을 확인
28. 표 셀에서 Enter → 셀 내부 줄바꿈과 행 높이 확장을 확인
29. 표 카드를 저장한 뒤 기존 카드와 새 카드 모두 셀 줄바꿈과 행 높이가 유지되는지 확인
30. 로그인 화면과 사이드바의 제목·설명을 각각 변경 → 반대쪽이 즉시 같은 값으로 동기화되고 레이아웃이 유지되는지 확인
31. 로그인 화면 텍스트 드래그 → `업데이트 등록` 버튼이 표시되지 않는지 확인
32. Viewer에서 메뉴 본문 텍스트 드래그 → `업데이트 등록` 버튼과 등록 팝업이 표시되지 않는지 확인
33. Editor에서 메뉴 본문 텍스트 드래그 → 버튼과 등록 팝업이 정상 동작하고, Viewer 전환 또는 로그아웃 시 즉시 닫히는지 확인
34. 로그인 후 사이드바에서 `업데이트` 바로 아래의 `도움말` 클릭 → 계층형 도움말 팝업이 열리고 Viewer·Editor 태그가 구분되어 표시되는지 확인
35. 도움말 팝업의 닫기·확인·Esc·배경 클릭 → 팝업이 닫히고 도움말 버튼으로 포커스가 복귀하는지 확인
36. 도움말 팝업의 레벨 1 토글 → 한 항목만 펼쳐지고, 레벨 2 토글 → 같은 레벨 1 안에서 여러 항목을 독립적으로 열고 닫을 수 있는지 확인

## 14. 작업 보고 형식

- 수정한 파일
- 수정한 기능과 적용 메뉴
- 변경 전 회귀 테스트 결과
- 변경 후 회귀 테스트 결과
- 브라우저에서 확인한 사용자 동작
- 실패한 테스트가 있으면 실패 원인
- 실패 시 적용하지 않은 변경 사항

## 15. Git 커밋 메시지 규칙

- 사용자가 언어를 별도로 지정하지 않으면 Git 커밋 제목과 본문을 한국어로 작성한다.
- 기술 명칭, 파일명, 함수명은 필요한 경우 원문을 유지한다.
